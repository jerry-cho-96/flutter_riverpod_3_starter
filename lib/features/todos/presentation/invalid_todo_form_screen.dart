import 'package:flutter/material.dart';

class InvalidTodoFormScreen extends StatelessWidget {
  const InvalidTodoFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('유효하지 않은 할 일 폼 경로입니다.')));
  }
}
